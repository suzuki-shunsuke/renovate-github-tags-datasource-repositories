{
  extends: ["config:base"],
  regexManagers: [
    {
      fileMatch: ["main.tf"],
      matchStrings: [
        '"(?<currentValue>.*?)" # renovate: depName=(?<depName>.*?)',
      ],
      datasourceTemplate: "github-tags",
    },
  ],
}
